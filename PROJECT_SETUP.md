🧱 PROJECT_SETUP.md

# 🚀 DGU App — Project Setup & Development Workflow

## 🧩 Общая идея

Проект DGU App построен по принципам **современной промышленной фронтенд-разработки**  
с упором на **надёжность, воспроизводимость и прозрачность изменений**.

Основная цель — уйти от ручного “прототипного” цикла к предсказуемому и управляемому **CI/CD workflow**  
с типизацией, линтингом и автоматическими проверками качества.

---

## ⚙️ Технологический стек

| Категория       | Технология                                              | Назначение                                                    |
| --------------- | ------------------------------------------------------- | ------------------------------------------------------------- |
| Язык            | **TypeScript**                                          | Строгая типизация и предотвращение ошибок на этапе компиляции |
| Фреймворк       | **React + Vite**                                        | Быстрая сборка и оптимизация фронтенда                        |
| IDE             | **VS Code**                                             | Основная среда разработки                                     |
| Линтер          | **ESLint + Prettier**                                   | Контроль качества и единый стиль кода                         |
| Контроль версий | **Git + GitHub**                                        | Версионирование, ветвление, ревью и защита main               |
| Pre-commit      | **Husky + lint-staged**                                 | Проверка кода до коммита                                      |
| CI/CD           | **GitHub Actions + Vercel**                             | Автоматическая проверка и деплой                              |
| Деплой          | **Vercel**                                              | Автоматические превью и публикация прод-версии                |
| Настройки IDE   | `.editorconfig`, `.prettierrc.json`, `eslint.config.js` | Единое форматирование у всех участников                       |

---

## 🧠 Архитектура проекта

src/
├─ components/ # Универсальные UI-компоненты (Card, Button, Input...)
├─ modules/ # Функциональные блоки приложения (Reservation, Settings, KPI...)
├─ lib/ # Бизнес-логика, селекторы, хелперы, утилиты, хранилище
├─ assets/ # Статические файлы, изображения, иконки
├─ App.tsx # Основной layout, таблицы, карточки, роутинг
└─ main.tsx # Точка входа Vite/React

---

## 🔄 Git Workflow (GitOps)

- Основная ветка: **`main`** (защищена от прямых пушей)
- Работа ведётся в отдельных ветках:
  - `feat/...` — новая функциональность
  - `fix/...` — исправления багов
  - `chore/...` — обслуживание и инфраструктура
- Все изменения проходят **Pull Request (PR)** → **CI-проверки** → **merge** в `main`.

### Стандартный цикл:

```bash
git switch main
git pull
git switch -c feat/new-feature
# работа в коде...
git add .
git commit -m "feat: реализовано бронирование с буфером"
git push -u origin feat/new-feature
# затем создаём Pull Request → CI → merge

🧪 CI/CD Pipeline

Pre-commit (локально):

Husky запускает lint-staged

Проверяются только изменённые файлы (ESLint, Prettier)

CI на GitHub Actions:

Запускается на push и pull_request

Проверки:

npm run lint

npm run typecheck

npm run build

Vercel Deploy:

Каждый Pull Request получает preview-деплой

После merge → автоматический прод-деплой из main

💎 Правила качества

Никаких any, кроме крайних случаев (и только с комментарием // eslint-disable-line @typescript-eslint/no-explicit-any)

Компоненты должны быть максимально чистыми и функциональными

Каждый файл имеет одну чёткую ответственность

Любой UI-элемент оформляется через shadcn/ui

Все новые функции покрываются хотя бы базовыми типами

🧰 Настройка среды VS Code

Рекомендуемые плагины:

ESLint

Prettier

TypeScript React (официальный)

GitLens

Tailwind CSS IntelliSense

Включи автоформатирование при сохранении:

File → Preferences → Settings → Format on Save ✅

🧭 Политика снапшотов и контрольных точек

Чтобы не потерять стабильные версии:

git tag -a snapshot_2025-11-08_v1.0 -m "Stable control point"
git push origin snapshot_2025-11-08_v1.0


Для отката:

git reset --hard snapshot_2025-11-08_v1.0
git push --force-with-lease

🧩 Краткое определение подхода

DGU App построен по принципам
Type-Safe, GitOps-Driven, CI/CD-Oriented Development
с автоматическим контролем качества и прозрачными ветками.

© 2025 DGU Automation Project — Architecture & Workflow maintained jointly
```
