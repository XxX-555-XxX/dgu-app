# 🧭 TRANSFER CONTEXT — проект DGU-APP

**Дата:** 08.11.2025  
**Назначение:** сохранение полного контекста проекта и рабочего подхода  
**Тип документа:** Технический и организационный паспорт проекта

---

## 1. Общая информация

**Название проекта:** `dgu-app`  
**Тип:** Web-приложение  
**Технологии:** React + TypeScript + Vite  
**Хостинг:** Vercel  
**CI/CD:** GitHub Actions + Husky  
**IDE:** Visual Studio Code  
**Менеджер пакетов:** npm  
**Основная ветка:** `main`  
**Рабочие ветки:** `feat/...`, `fix/...`, `chore/...`

---

## 2. Текущий технологический стек

| Категория          | Используемые инструменты                                |
| ------------------ | ------------------------------------------------------- |
| **Frontend**       | React, TypeScript, Vite                                 |
| **Инфраструктура** | GitHub Actions, Husky, lint-staged, ESLint v9, Prettier |
| **Окружение**      | Node.js 20+, npm                                        |
| **Хостинг**        | Vercel (Preview = PR, Production = main)                |
| **CI Workflow**    | `.github/workflows/ci.yml` — lint → typecheck → build   |

---

## 3. Структура проекта

dgu-app/
│
├── .github/workflows/ci.yml # CI pipeline
├── .husky/ # Git hooks
├── docs/
│ ├── ESLint_TODO_Report.md # список техдолга по ESLint
│ └── README.md # якорь каталога docs
│
├── src/ # исходники приложения
│ ├── components/
│ ├── modules/
│ ├── lib/
│ └── main.tsx
│
├── eslint.config.js
├── package.json
├── PROJECT_SETUP.md
├── CONTRIBUTING.md
├── README.md # основной README со ссылками на docs
└── vite.config.ts

markdown
Копировать код

---

## 4. CI/CD Pipeline

**Husky (pre-push):**

- Для feature-веток выполняет: `npm run typecheck` и `npm run build`
- Для main/release-веток — полный цикл `lint + typecheck + build`

**GitHub Actions (`ci.yml`):**

- `Lint` — неблокирующий (warnings не валят билд)
- `Typecheck` — обязательный
- `Build` — обязательный
- Автоматически запускается при `push` и `pull request` в `main`

**Vercel:**

- Production branch → `main`
- Preview → каждая feature-ветка (PR)
- Build command → `npm run build`
- Output directory → `dist`

---

## 5. Среда разработки

**Основное окружение:**

- Visual Studio Code
- Node.js 20+
- TypeScript 5+
- ESLint 9+
- Prettier
- Git + GitHub
- Vercel

**npm scripts:**

```bash
npm run dev        # локальная разработка
npm run build      # сборка проекта
npm run typecheck  # проверка типов
npm run lint       # линтинг кода
6. Документация проекта
Файл	Описание
PROJECT_SETUP.md	Установка, окружение и общие команды
CONTRIBUTING.md	Правила веток, коммитов и PR
docs/ESLint_TODO_Report.md	Список ошибок линтера (техдолг)
docs/README.md	Описание каталога документации
README.md	Основная документация и навигация

7. Рабочие принципы и формат взаимодействия
Работаем локально в VS Code.
ChatGPT не использует Canvas, предпросмотр или виртуальную среду.
Все изменения выполняются вручную на компьютере пользователя.

ChatGPT всегда присылает полный файл под замену,
с указанием пути, например:
src/modules/ReservationDialog.tsx — заменить полностью.

Порядок действий при изменении:

bash
Копировать код
git add путь/к/файлу
git commit -m "fix: описание изменений"
git push
CI и Husky автоматически выполняют проверки (lint, typecheck, build).
Ошибки исправляются до успешного прохождения CI.

Работа через ветки и PR:

Новая задача → новая ветка (fix/..., feat/...)

После правок → Push → GitHub Actions → Vercel Preview → Merge

Контроль контекста чата:
При заполнении контекста на 80% ChatGPT предупреждает
и формирует срез (“snapshot”) проекта для безопасного переноса.

Формат ответов:

Без воды, кратко и по делу

Пошаговые инструкции

Только готовые решения, без гипотез

Код — всегда целиком, без вставок

8. Текущее состояние
✅ CI полностью настроен и работает
✅ Husky и lint-staged функционируют корректно
✅ main защищена — push запрещён, merge только через PR
✅ Vercel корректно деплоит Preview и Production
✅ Все ESLint и TypeScript ошибки собраны в docs/ESLint_TODO_Report.md
✅ Среда разработки и пайплайн стандартизированы

9. Назначение документа
Этот файл служит:

Как "паспорт проекта" — для новых разработчиков и будущих этапов

Как “transfer-промт” — при переносе в новый чат ChatGPT

Как эталон рабочего подхода для других проектов

10. Как использовать при переносе
Открой новый чат ChatGPT

Скопируй содержимое этого файла

Вставь в чат и напиши:

"Продолжаем работу над проектом DGU-APP, текущая ветка fix/types-and-imports"

ChatGPT мгновенно восстановит контекст, стиль и пайплайн

Автор: Виталий (основной разработчик проекта)
Технический куратор: ChatGPT (GPT-5)
Дата последнего обновления: 08.11.2025
```
