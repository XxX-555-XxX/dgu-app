## 💡 Общая идея

Проект использует **современный GitHub Flow**:  
каждая задача — отдельная ветка → проверка через CI + Vercel Preview → merge в `main` → автодеплой.

---

## ⚙️ Основной процесс

### 1. Создание ветки

Всегда начинай работу от актуального `main`:

```bash
git switch main
git pull
git switch -c feat/my-new-feature
Префиксы веток:

feat/ — новая функциональность

fix/ — исправление ошибок

chore/ — технические задачи (CI, конфигурация, скрипты)

docs/ — документация

2. Разработка и проверка
Локально доступны команды:

Команда	Назначение
npm run dev	Локальный сервер разработки
npm run lint	Проверка кода (ESLint, non-blocking)
npm run typecheck	Проверка типов (TypeScript)
npm run build	Сборка проекта (Vite)

Перед пушем желательно прогнать typecheck и build.

3. Пуш и CI
При пуше ветки GitHub Actions выполняет:

Lint — проверка форматирования и стиля (предупреждения не блокируют);

Typecheck — строгая проверка типов;

Build — сборка проекта.

Если все шаги зелёные ✅ — значит, код готов к обзору.

4. Pull Request (PR)
Создай PR в main.
GitHub автоматически добавит CI-статусы и Vercel-ссылку:

✅ CI: lint + typecheck + build

🌐 Vercel Preview: ссылка на тестовую сборку (например, fix-my-feature.vercel.app)

После проверки и апрува можно мержить.

5. Деплой в продакшн
После Merge в main:

GitHub Actions запускает CI.

Vercel автоматически собирает Production Deployment.

Если нужно пересобрать прод с нуля:

Зайди в Vercel → Deployments.

Нажми Redeploy.

Сними галочку “Use existing Build Cache”.

6. Снапшоты и откаты
Для стабильных точек:

bash
Копировать код
git tag -a snapshot_2025-11-08_v1 -m "Stable snapshot"
git push origin snapshot_2025-11-08_v1
Откат:

bash
Копировать код
git checkout main
git reset --hard snapshot_2025-11-08_v1
git push --force-with-lease
В Vercel также можно сделать “Revert to this deployment”.

🧰 Дополнительные правила
Прежде чем пушить в main — всегда через PR.

Мерж разрешён только после зелёных статусов CI.

Force-push и прямые изменения в main заблокированы политикой GitHub (ruleset).

🏷️ Контрольные точки
Перед мержем проверяй:

✅ CI зелёный

✅ Vercel Preview работает

✅ Нет критических ошибок в npm run typecheck

🧹 Все правки оформлены чистыми коммитами

🏷️ (опционально) Создан snapshot-тег

Хорошая практика — писать коммиты в едином формате:

scss
Копировать код
feat(ui): add KPI chart component
fix(types): correct Reservation typing
chore(ci): optimize workflow caching
```
